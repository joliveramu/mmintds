// Write a JavaScript program that will display a
// remark of “passed” if the student grade is greater
// than or equal to 70, otherwise, the remark is
// “failed.”

let grade = 95;

if(grade>=70)
{
	alert(`passed`);
}else{
	alert(`failed.`);
}